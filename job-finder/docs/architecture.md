# JobFinder Architecture

- Frontend: vanilla HTML/CSS/JS with modular files by domain
- Backend: Django with domain apps (`users`, `jobs`, `applications`)
- API base routes: `/api/auth/`, `/api/jobs/`, `/api/applications/`
