# JobFinder

JobFinder is a structured job search platform project split into:

- `frontend/` for HTML, CSS, and vanilla JavaScript
- `backend/` for Django + Django REST Framework
- `docs/` for architecture and workflow notes

## Frontend preview

Open `frontend/pages/home.html` directly, or serve `frontend/` using any static server.
